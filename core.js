/**
 * shared code for all page
 */
$(document).ready(function () {
	$(".sidenav").sidenav();
});

$(".dropdown-trigger").dropdown({
	hover: true
});
$(document).ready(function () {
	// create mobile menu

	$('.sidenav').sidenav();
	// create dropdown
});