/**
 * shared code for all page
 */
$(document).ready(function() {
    $(".sidenav").sidenav();
});

$(document).ready(function() {
    // create mobile menu

    $('.sidenav').sidenav();
    // create dropdown
});